#include <stdio.h>
#include <fcntl.h>
#define BUFSIZE 512

int main(int argc, char *argv[])
{ 
// 작성 필요한 fd 및 변수 -----
// 작성 필요한 변수 -----

	if (argc != 3) { 
		fprintf(stderr,"사용법: %s file1 file2\n", argv[0]); 
		exit(1); 
	}

// 복사할 원본 파일 열기 / 복사할 대상 파일 열기 / 읽기 / 쓰기 시작 -----








// 복사할 원본 파일 열기 / 복사할 대상 파일 열기 / 읽기 / 쓰기 종료 -----

	exit(0); 
}

